# Task 3-B: Accessibility and Semantic HTML Fixes

## Files Modified
1. **Header.tsx** — `<a>` desktop nav links, Escape key, focus management, aria-label fix, WhatsApp sr-only
2. **Footer.tsx** — `<a>` nav links, `<address>` element, removed scrollToSection
3. **TestimonialsSection.tsx** — `<blockquote>`, `<figure>`, `<figcaption>`
4. **TeamSection.tsx** — `<article>` for professional cards
5. **ServicesSection.tsx** — `<article>` for service cards
6. **ConveniosSection.tsx** — Pause on hover/focus for marquee (WCAG 2.2 SC 2.2.2)
7. **globals.css** — `.group:hover` / `.group:focus-within` animation-play-state: paused
8. **ScrollReveal.tsx** — prefers-reduced-motion bypass
9. **AmbientSound.tsx** — aria-hidden on icons and ping
10. **OrganicDivider.tsx** — aria-hidden on 'o' variant
11. **ConceptSection.tsx** — aria-hidden on decorative SVG

## Lint Result
0 errors, 1 pre-existing warning
